/** SPA 向け再エクスポート（実装は shared/reservation） */
export {
  MAX_LINE_MESSAGE_CHARS,
  MAX_MEMO_CHARS,
  MAX_RESERVATIONS_IN_MESSAGE,
  buildReservationLineMessage,
} from '../../../shared/reservation/buildLineMessage.js'
