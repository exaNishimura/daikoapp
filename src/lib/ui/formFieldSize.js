/**
 * 売上管理（Receivables / 請求）以外の業務フォーム共通の入力サイズ。
 * Astryx TextInput / DateInput / Selector / Typeahead / TextArea の size に渡す。
 * 売上系の密な表インライン編集はこれまで通り `sm` を個別指定する。
 */
export const FORM_FIELD_SIZE = 'md'
